# cesium瓦片加载问题

TMS瓦片和WMTS瓦片的区别，一个是社区协议不规范，一个是标准协议，先访问规则，在根据规则请求瓦片。TMS要手动根据规则加载。

在 Cesium 里加载**经纬度（EPSG:4326）的 TMS 影像**，核心点只有三个：
 坐标系是 `GeographicTilingScheme`，Y 轴方向通常要翻，级别范围要对齐服务端。

```ts
import {
  UrlTemplateImageryProvider,
  GeographicTilingScheme
} from 'cesium';

const imageryProvider = new UrlTemplateImageryProvider({
  url: 'https://example.com/tiles/{z}/{x}/{reverseY}.png',
  tilingScheme: new GeographicTilingScheme(),
  minimumLevel: 0,
  maximumLevel: 18
});

```

**`{reverseY}`**
 大多数 TMS（原始 TMS 规范）Y 轴是从下往上的，Cesium 默认是从上往下。
 👉 能显示但上下颠倒 = 把 `{y}` 改成 `{reverseY}`。

一般加载墨卡托TMS 影像按照cesium默认就行，如果有问题可以尝试使用 reverseY，服务最小级别设置为1